<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Burger</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="..views/style.css">
  </head>
  <body>
    {{{body}}}
    <script>
// all working javascript for the button and what not should go here? or on a seperate javascript file?
//on this event, the function runs to show burger info user just typed
$("burgerbutton").click(function(){

  // write a function that appends and show database burger that user just typed into the input box
  var showburgers = function() {
    // if user types something in input box
    // then append results to ('whateverinamedhtmlID')
    // and also then send that info to database
  };
  //call the function here 
  showburgers();
  });

    </script>
  </body>
</html>
